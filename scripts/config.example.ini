[odoo]
# Odoo connection settings
url = http://localhost:8069
database = dtx_dev
username = admin
password = admin

[setup]
# Skip if already exists
skip_existing = true

# Create sample data (products, customers, etc.)
create_samples = false

# Company settings
company_name = DTX - Smart Queue Management Systems
company_currency = VND
