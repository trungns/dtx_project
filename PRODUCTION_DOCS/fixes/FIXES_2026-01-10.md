# Sửa lỗi PAKD và dọn dẹp Git - 2026-01-10

## Tóm tắt

Đã sửa 3/4 lỗi bạn báo cáo cho SO0158 và dọn dẹp Git.

## ✅ Các lỗi đã sửa

### 1. CEO và GDKD có thể sửa PAKD sau khi duyệt ✅

**Vấn đề:** PAKD sau khi duyệt bị readonly cho tất cả user, kể cả CEO và GDKD.

**Giải pháp:**
- Xóa `attrs="{'readonly': [('state', '=', 'approved')]}"` khỏi view
- Logic phân quyền đã có sẵn trong Python `write()` method
- Chỉ CEO và GDKD mới sửa được PAKD đã duyệt

**Files thay đổi:**
- [dtx_pakd_views.xml](odoo-dev/addons/dtx_sales_pakd_contract/views/dtx_pakd_views.xml)

---

### 3. Import chi phí từ PAKD báo lỗi ✅

**Vấn đề:** Import chi phí từ PAKD bị lỗi vì cố tạo contract cost từ section/note lines (không có product_id, uom_id).

**Giải pháp:**
- Skip section/note lines (`display_type != False`)
- Skip lines thiếu product hoặc uom
- Add fallback cho name field

**Files thay đổi:**
- [sale_order.py:360-382](odoo-dev/addons/dtx_sales_pakd_contract/models/sale_order.py#L360-L382)

**Code:**
```python
# Create contract costs from PAKD lines
sequence = 10
for pakd_line in pakd.line_ids:
    # Skip section/note lines
    if pakd_line.display_type:
        continue

    # Skip lines without product or uom
    if not pakd_line.product_id or not pakd_line.uom_id:
        continue

    self.env['dtx.contract.cost'].create({
        'sale_order_id': self.id,
        'sequence': sequence,
        'product_id': pakd_line.product_id.id,
        'name': pakd_line.name or pakd_line.product_id.display_name,
        'qty': pakd_line.qty,
        'uom_id': pakd_line.uom_id.id,
        'planned_unit_cost': pakd_line.purchase_unit_price,
        'actual_unit_cost': pakd_line.purchase_unit_price,
        'cost_type': 'planned',
    })
    sequence += 10
```

---

### 4. Import báo giá (Tạo PAKD) báo lỗi ✅

**Vấn đề:** Button "Tạo PAKD mới" bị lỗi khi báo giá không có sản phẩm.

**Giải pháp:**
- Validate báo giá phải có ít nhất 1 product line
- Skip lines không có product_id
- Hiển thị thông báo lỗi rõ ràng

**Files thay đổi:**
- [sale_order.py:269-298](odoo-dev/addons/dtx_sales_pakd_contract/models/sale_order.py#L269-L298)

**Code:**
```python
def action_create_pakd(self):
    """Create new PAKD from Sales Order lines"""
    self.ensure_one()

    # Check if there are any product lines
    product_lines = self.order_line.filtered(lambda l: not l.display_type and l.product_id)
    if not product_lines:
        raise UserError(
            'Báo giá không có sản phẩm nào để tạo PAKD.\n'
            'Hãy thêm sản phẩm vào báo giá trước khi tạo PAKD.'
        )
    ...
```

---

## ❓ Lỗi #2: Điền số hợp đồng và lưu bị lỗi

**Trạng thái:** Chưa có đủ thông tin để fix

**Vấn đề:** Bạn báo "Tôi điền Số hợp đồng và lưu thì bị lỗi"

**Phân tích:**
- Field `x_contract_no` là Char field đơn giản, không có constraint
- Không có lý do kỹ thuật nào field này bị lỗi
- Cần thông tin thêm:
  - Message lỗi chính xác là gì?
  - Lỗi xảy ra khi nào? (Draft? Confirmed? Sau khi confirm?)
  - User đang dùng có quyền gì? (Sales User? Director? CEO?)

**Cần làm để debug:**
1. Mở Odoo log file xem message lỗi chi tiết
2. Hoặc chụp screenshot lỗi gửi cho tôi
3. Test với user khác (CEO/Sales Director) xem có bị lỗi không

**Nếu muốn tôi fix ngay:**
- Cung cấp error message từ Odoo log
- Hoặc screenshot lỗi
- Tôi sẽ fix ngay

---

## 🧹 Dọn dẹp Git

**Đã xóa 18 files khỏi Git:**

### Batch 1 (commit 2080c13):
- ODOO_18_MIGRATION_GUIDE.md
- PAKD_FORMULAS_FIXED.md
- PAKD_FORMULA_ANALYSIS.md
- PAKD_FORMULA_FIX_SUMMARY.md
- SESSION_2026_01_04_PAKD_FORMULA_FIX.md
- SESSION_SUMMARY_2025-12-28.md
- odoo-dev/addons/dtx_sales_pakd_contract/MANUAL_UAT_GUIDE.md
- odoo-dev/addons/dtx_sales_pakd_contract/docs/MANUAL_UAT_TEST_CASES.md
- odoo-dev/addons/dtx_sales_pakd_contract/tests/test_uat_quy_chau.py
- odoo-dev/addons/dtx_serial_ext/scripts/recheck_vendor_invoices.py

### Batch 2 (commit e18d011):
- FINAL_FIX_SUMMARY.md
- FIX_ACCOUNTING_ACCOUNTS.md
- KIOSK_PRODUCTION_TEST_FLOW.md
- README_HANDOFF.md
- ROOT_CAUSE_FINAL.md
- SUBCONTRACTING_STANDARD_WORKFLOW.md
- SUBCONTRACTING_WORKAROUND.md
- TEST_DATA_SUMMARY.md

**Kết quả:** Git repository giờ sạch sẽ, chỉ còn code và documentation quan trọng trong PRODUCTION_DOCS/

---

## 📦 Module Version Update

**Version:** 1.3.0 → 1.4.0

**Changelog trong __manifest__.py:**
```python
Version 1.4.0:
- **FIX**: Allow CEO and GDKD to edit approved PAKD (remove view readonly attrs)
- **FIX**: Skip section/note lines in import_pakd_costs (prevent errors)
- **FIX**: Better validation in action_create_pakd (check for product lines)
- **FEATURE**: Add commission tracking (x_customer_commission, x_referrer_commission)
- **FEATURE**: Add net profit fields (x_net_profit, x_net_profit_margin)
```

---

## 🚀 Bước tiếp theo

### Để test ngay trên MacBook:

1. **Restart Odoo:**
   ```bash
   docker-compose restart odoo
   ```

2. **Upgrade module:**
   - Vào Apps > DTX Sales PAKD Contract
   - Click Upgrade
   - Hoặc chạy: `docker-compose exec odoo odoo -u dtx_sales_pakd_contract -d dtx_dev --stop-after-init`

3. **Test các fix:**
   - ✅ Test CEO/GDKD sửa PAKD đã duyệt
   - ✅ Test import chi phí từ PAKD (với PAKD có section/note)
   - ✅ Test tạo PAKD mới từ báo giá trống
   - ❓ Test điền số hợp đồng và lưu → Báo lỗi nếu còn

### Để làm tiếp trên Windows:

1. **Pull code từ GitHub:**
   ```bash
   git pull origin main
   ```

2. **Setup Odoo 16 trên Windows:**
   - Follow hướng dẫn: [PRODUCTION_DOCS/01_INSTALLATION/01_INSTALLATION_GUIDE.md](PRODUCTION_DOCS/01_INSTALLATION/01_INSTALLATION_GUIDE.md)

3. **Restore database:**
   - Dùng script restore đã có
   - Hoặc copy database từ MacBook

4. **Chạy setup scripts:**
   ```bash
   cd scripts
   cp config.example.ini config.ini
   # Edit config.ini với connection details
   python run_all_setup.py
   python utils/verify_setup.py
   ```

---

## 📝 Notes

- Tất cả changes đã commit vào Git
- Repository đã clean, ready for production
- Module version 1.4.0 ready to deploy
- Còn issue #2 cần thêm thông tin để fix

---

## 💬 Nếu cần hỗ trợ thêm

Hãy cho tôi biết:
1. Message lỗi chính xác khi điền số hợp đồng
2. Screenshot lỗi nếu có
3. Odoo log file (`odoo.log`) nếu có

Tôi sẽ fix ngay! 🚀
